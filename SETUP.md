# راهنمای راه‌اندازی

## مراحل راه‌اندازی

### 1. نصب وابستگی‌ها
```bash
npm install
```

### 2. تنظیم فایل پروفایل اصلی
فایل `data/master-profile.ovpn` را با فایل پروفایل OpenVPN واقعی خود جایگزین کنید. این فایل به عنوان الگو برای تولید فایل‌های پروفایل کاربران استفاده می‌شود.

### 3. اجرای برنامه
```bash
npm run dev
```

برنامه در آدرس `http://localhost:3000` در دسترس خواهد بود.

### 4. ورود به پنل مدیریت
- آدرس: `http://localhost:3000/admin`
- نام کاربری پیش‌فرض: `admin`
- رمز عبور پیش‌فرض: `admin123`

⚠️ **هشدار امنیتی**: حتماً رمز عبور پیش‌فرض را تغییر دهید!

## نحوه استفاده

### برای کاربران:
1. وارد صفحه ثبت نام شوید (`/register`)
2. شماره موبایل، کد ملی و نام معرف را وارد کنید
3. کد رهگیری ۱۰ حرفی را دریافت و ذخیره کنید
4. برای پیگیری درخواست، به صفحه `/track` بروید و کد رهگیری را وارد کنید
5. پس از تایید درخواست توسط ادمین، می‌توانید اطلاعات اتصال و فایل پروفایل را دانلود کنید

### برای ادمین:
1. وارد پنل مدیریت شوید (`/admin`)
2. درخواست‌های کاربران را مشاهده کنید
3. برای هر درخواست در انتظار، نام کاربری و رمز عبور را وارد کنید
4. روی دکمه "تایید" کلیک کنید
5. سیستم به صورت خودکار:
   - فایل پروفایل را با نام رندوم در فولدرهای تو در تو ایجاد می‌کند
   - اطلاعات را در دیتابیس ذخیره می‌کند
   - لینک دانلود فایل را در اختیار قرار می‌دهد

## ساختار دیتابیس

### جدول users:
- `id`: شناسه یکتا
- `mobile`: شماره موبایل (یکتا)
- `national_id`: کد ملی (یکتا)
- `referrer_name`: نام معرف
- `tracking_code`: کد رهگیری ۱۰ حرفی (یکتا)
- `status`: وضعیت (pending, approved, rejected)
- `username`: نام کاربری VPN
- `password`: رمز عبور VPN
- `profile_file_path`: مسیر فایل پروفایل
- `created_at`: تاریخ ایجاد
- `updated_at`: تاریخ به‌روزرسانی

### جدول admins:
- `id`: شناسه یکتا
- `username`: نام کاربری ادمین
- `password_hash`: هش رمز عبور
- `created_at`: تاریخ ایجاد

## نکات امنیتی

1. **تغییر رمز عبور ادمین**: پس از اولین ورود، رمز عبور را تغییر دهید
2. **JWT_SECRET**: در فایل `.env` یک کلید امنیتی قوی تنظیم کنید
3. **HTTPS**: در محیط production حتماً از HTTPS استفاده کنید
4. **Rate Limiting**: برای جلوگیری از سوء استفاده، rate limiting اضافه کنید
5. **اعتبارسنجی**: ورودی‌های کاربر را به دقت اعتبارسنجی کنید

## عیب‌یابی

### خطای "Master profile file not found"
- مطمئن شوید فایل `data/master-profile.ovpn` وجود دارد

### خطای دیتابیس
- فایل `data/vpn-share.db` را حذف کنید تا دوباره ایجاد شود
- دقت کنید که پوشه `data` قابل نوشتن باشد

### خطای دسترسی به فایل
- مطمئن شوید پوشه `data/profiles` قابل نوشتن است

